val nil ()

func list (args)

func foreach (
    (if (cadr-args)
        (quote (nil
            ((caar-args) (caadar-args))
            (foreach (caar-args) (cdadar-args))
        ))
        nil
    )
    (args)
)

val main (foreach print
    (list
        "look at this"
        "It parses and executes"
        "Using if, c[ad]+r, and recursion"
    )
)
